#include <iostream>
#include <limits.h>
#include <vector>
using namespace std ;
int peakElement(vector<int> nums , int target , int &ans )
{
  int n = nums.size();
  int s = 0 ;
  int e = n - 1 ;
  int mid = s + (e-s)/2 ;
  for (int i = 0 ; i < n ; i++)
  {
    if (max(target , nums[mid]));
    {  
      ans =              
    }

  }
}     D